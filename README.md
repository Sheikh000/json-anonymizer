# JSON Anonymizer

Phase 1 TypeScript starter for a JSON anonymizer library. Implements scaffolding for masking sensitive fields based on an explicit config. Fill in algorithms and tests.

Run tests:

```powershell
npm test
```
# json-anonymizer